# SmartAssist Pipeline Python Dependencies
# Location: pipeline/requirements.txt
# Compatible with Python 3.8+ on NVIDIA Jetson Orin

# Core dependencies
numpy>=1.21.0,<2.0.0
PyYAML>=6.0
pandas>=1.3.0

# CAN bus communication
python-can>=4.0.0
cantools>=38.0.0

# Azure storage (for log upload)
azure-storage-blob>=12.14.0

# Utilities
python-dateutil>=2.8.0
pytz>=2021.3

# GStreamer Python bindings
# NOTE: Install via apt, not pip
#   sudo apt-get install python3-gi python3-gst-1.0

# System dependencies (install via apt):
# - gstreamer1.0-tools
# - gstreamer1.0-plugins-good
# - gstreamer1.0-plugins-bad
# - libgstrtspserver-1.0-dev

# NVIDIA dependencies (pre-installed on Jetson):
# - DeepStream SDK 6.4+
# - CUDA 12.2+
# - TensorRT 8.6+
# - PyDS (DeepStream Python bindings)