# SmartAssist CAN Server Python Dependencies
# Location: services/can-server/requirements.txt
# Compatible with Python 3.8+

# CAN bus communication
python-can>=4.0.0
cantools>=38.0.0

# Core dependencies
numpy>=1.21.0,<2.0.0
pandas>=1.3.0

# System dependencies (install via apt):
# - can-utils
#   sudo apt-get install can-utils